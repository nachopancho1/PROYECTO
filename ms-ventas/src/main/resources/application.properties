spring.application.name=ms-ventas
server.port=8084

# Conexión corregida con parámetro de auto-creación para db_ventas
spring.datasource.url=jdbc:mysql://localhost:3306/db_ventas?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=

# Hibernate: Configuración estándar
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- CONFIGURACIÓN DE SWAGGER (AGREGA ESTO) ---
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api/ventas/v3/api-docs